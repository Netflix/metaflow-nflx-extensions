name: nflxext-dev
category: dev
channels:
  - conda-forge
  - nodefaults
dependencies:
  - pytest
  - pytest-cov
  - pytest-xdist
  - pytest-timeout
  - pytest-mock
  - wheel
  - mamba
  - conda
  - conda-lock
  - conda-package-handling
  - coverage
  - pandas
  - pip:
    - sh

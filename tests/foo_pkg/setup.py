from setuptools import setup

setup(
    name="foo",
    version="1.0.0",
    author="Romain",
    description='A simple Python package that prints "Hello, World!"',
    packages=["foo_pkg"],
    install_requires=[],
)

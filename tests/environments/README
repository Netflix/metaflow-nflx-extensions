To test environment resolution we have tests in two places:
  - environments: this contains expensive tests that do full environment
      resolutions.
  - plugins/conda: this contains more unit level tests. These tests are
      faster and do not perform any resolution. Tests here may refer to
      environment descriptions in environments
